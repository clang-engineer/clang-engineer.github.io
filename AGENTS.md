# Repository Instructions

## Documentation

이 저장소의 한국어 기술 문서를 작성하거나 수정할 때 [`docs/WRITING_STYLE.md`](docs/WRITING_STYLE.md)의 공통 문체·표기 원칙을 따른다.

하위 디렉터리에 별도의 작성 규칙이 있으면 저장소 공통 원칙과 함께 해당 규칙을 적용한다. 하위 규칙은 문서의 목적, 구조, 학습 방식처럼 해당 영역에만 필요한 내용을 정의하고, 공통 문체·용어 규칙을 중복해서 복사하지 않는다.

기존 문서를 수정할 때는 내용의 정확성뿐 아니라 기존 용어와 맥락의 일관성도 유지한다.

### 문서 작성 전 규칙 확인

문서 생성·수정을 시작하기 전에 적용되는 규칙 문서를 **실제로 읽고 확인한다.** 규칙 문서가 다른 상위 공통 규칙을 참조하면 링크가 있다는 사실만 확인하고 넘어가지 말고, 참조된 규칙까지 확인한다.

특히 `_archive/license/정보관리기술사/세부학습/` 문서를 생성·수정할 때는 다음 순서로 확인한다.

1. [`docs/WRITING_STYLE.md`](docs/WRITING_STYLE.md) — 저장소 공통 문체·용어·표기 원칙
2. [`_archive/license/정보관리기술사/세부학습/00-세부학습-작성규칙.md`](_archive/license/정보관리기술사/세부학습/00-세부학습-작성규칙.md) — 세부학습 전용 작성 원칙
3. 기존 대상 문서 — 이미 사용 중인 용어, 설명 방식, 이해 흐름

`00-세부학습-작성규칙.md`가 공통 문체·표기 원칙을 참조하고 있더라도 **참조만으로 확인을 대신하지 않는다.** 이미 자연스럽게 사용하고 있고 기술적으로 문제가 없는 용어는 임의의 쉬운 표현이나 번역어로 치환하지 않는다.

### 정보관리기술사 세부학습의 깊이와 범위

`_archive/license/정보관리기술사/세부학습/` 문서는 **개념을 이해하고 정보관리기술사 답안을 구성하는 데 필요한 깊이까지만 다룬다.** 정확성을 위해 필요한 설명은 충분히 하되, 관련 있다는 이유만으로 구현체·제품·논문 수준의 세부사항까지 계속 확장하지 않는다.

- 기본 범위는 `정의 → 왜 필요한가 → 핵심 구조·동작 원리 → 주요 구성요소 → 대표 비교 → 장단점·Trade-off → 기술사 수준의 적용·고려사항 → 헷갈리기 쉬운 경계`다.
- 새로운 세부 개념을 추가하기 전에 **“이 내용을 빼면 핵심 개념을 잘못 이해하는가?” 또는 “정보관리기술사 답안 구성에 실질적으로 필요한가?”**를 확인한다. 둘 다 아니라면 원칙적으로 생략한다.
- 내부 Algorithm 세부, 수식 전개, 버전별 구현 차이, 특정 제품 옵션, 구현체 내부 구조는 핵심 이해나 기술사 수준의 설명에 꼭 필요한 경우에만 포함한다.
- 논문 수준의 수학적 전개, 특정 구현의 지나친 최적화 세부, 예외 사례의 장시간 나열, 단순히 흥미로운 주변 지식은 기본 범위에서 제외한다.
- 깊이가 필요한 내용이더라도 본문의 핵심 흐름을 깨면 별도 심화 문서나 참고 항목으로 분리하고, 기본 세부학습 문서를 백과사전처럼 만들지 않는다.
- 문서가 길다는 이유만으로 기존의 이해에 필요한 설명을 삭제하지 않는다. **깊이 제한은 핵심 설명을 압축하라는 뜻이 아니라, 핵심에서 불필요하게 더 내려가지 말라는 뜻**이다.

### 정보관리기술사 세부학습 Diagram

`_archive/license/정보관리기술사/세부학습/` 문서에서는 **Text/ASCII Diagram을 원칙으로 사용하고 Mermaid는 특별한 경우가 아니면 사용하지 않는다.**

- 짧은 Pipeline, 계층, 비교, 반복 흐름, 일반적인 Architecture는 Text/ASCII로 표현한다.
- Mermaid는 관계가 복잡해 Text/ASCII로 표현했을 때 오히려 이해가 크게 떨어지고, 시각화 자체가 학습에 실질적인 이점을 주는 경우에만 예외적으로 사용한다.
- Mermaid를 사용하지 못하는 GitHub 모바일 앱, Terminal, 일반 Markdown Viewer에서도 핵심 내용을 완전히 이해할 수 있어야 한다.
- 기존 세부학습 문서를 수정할 때 단순한 Mermaid Diagram이 있으면 가능하면 Text/ASCII Diagram으로 교체한다.

## Safe Editing

기존 파일 수정에서는 **원문 보존을 최우선**으로 한다.

- 일부 구간만 수정하는 요청이면 해당 구간만 바꾸고, 나머지 본문을 요약·축약·재작성하지 않는다.
- Tool이나 API 응답이 `truncated`, 일부 line만 반환, 중간 생략 등으로 **전체 파일인지 확실하지 않으면 그 내용을 전체 원문으로 간주하지 않는다.**
- 전체 파일을 교체하는 API를 사용할 때는 쓰기 전에 **완전한 현재 원문을 확보했는지 확인**한다. 완전한 원문을 확보하지 못했으면 전체 교체를 수행하지 않는다.
- 긴 문서가 잘렸다고 의심되면 먼저 현재본과 Git history를 비교한다. 이전 정상본이 있으면 원래 내용을 복구하고, 최초 생성부터 잘린 경우에만 문맥을 근거로 새 결말을 작성한다.
- 기존 상세 문서를 짧은 요약본으로 바꾸지 않는다. 문서 축약은 사용자가 명시적으로 요청한 경우에만 수행한다.
- 수정 전후 diff에서 요청 범위와 무관한 대량 삭제·대량 축약이 발생하면 작업을 중단하고 원인을 확인한다.
- 쓰기 후에는 파일의 **시작부, 수정 구간, 끝부분**을 다시 읽어 원문 손실과 문장 절단이 없는지 확인한다.
- Markdown 문서는 쓰기 후 Code Fence, Table, List, Heading이 중간에 끊기지 않았는지도 확인한다.
- 예상하지 못한 내용 손실이 발생하면 추가 수정을 이어가지 말고 직전 정상 Commit/Blob을 기준으로 즉시 복구한다.
