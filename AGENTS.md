# Repository Instructions

## Documentation

이 저장소의 한국어 기술 문서를 작성하거나 수정할 때 다음 공통 규칙을 함께 적용한다.

1. [`docs/WRITING_STYLE.md`](docs/WRITING_STYLE.md) — 공통 문체·용어·표기 원칙
2. [`docs/DOCUMENT_TYPES.md`](docs/DOCUMENT_TYPES.md) — 문서 목적과 유형별 구조 원칙
3. [`docs/KNOWLEDGE_ORGANIZATION.md`](docs/KNOWLEDGE_ORGANIZATION.md) — 지식 단위·관계·Knowledge/Blog 역할 원칙

문서를 작성하거나 크게 수정하기 전에 먼저 **문서의 목적과 주 유형을 결정한다.** 모든 글에 같은 형식을 강제하지 않는다. Roadmap·Concept·Architecture·Comparison처럼 이해가 목적인 문서는 거시에서 미시로 내려가고, How-to·Troubleshooting·Record처럼 수행·해결·기록이 목적인 문서는 해당 목적에 맞는 흐름을 우선한다.

하위 디렉터리에 별도의 작성 규칙이 있으면 저장소 공통 원칙과 함께 해당 규칙을 적용한다. 하위 규칙은 문서의 목적, 구조, 학습 방식처럼 해당 영역에만 필요한 내용을 정의하고, 공통 문체·용어 규칙을 중복해서 복사하지 않는다.

기존 문서를 수정할 때는 내용의 정확성뿐 아니라 기존 용어와 맥락의 일관성도 유지한다.

### 규칙 추가 원칙

새로운 문서 규칙을 추가하기 전에 **기존 규칙으로 해결할 수 없는 실제 실패 사례나 반복되는 문제가 있는지 먼저 확인한다.** 단순히 더 정교해 보이거나 발생 가능성이 있다는 이유만으로 규칙을 늘리지 않는다.

- 기존 공통 규칙이나 하위 규칙의 적용으로 해결할 수 있으면 새 규칙을 만들지 않는다.
- 같은 취지의 규칙이 여러 문서에 필요하면 복제하지 말고 가장 상위의 적절한 공통 규칙에 둔다.
- 특정 영역에만 필요한 제약은 공통 규칙으로 올리지 않고 해당 하위 규칙에 둔다.
- 새로운 규칙이 문서 작성 비용을 늘리는 만큼 실제 오류·혼란을 줄이는지 확인한다.

### 규칙 강도 표현

규칙의 강도는 문장 표현으로 일관되게 구분한다.

- `반드시`, `금지`, `하지 않는다` → 강제 규칙
- `원칙적으로`, `우선한다`, `기본으로 한다` → 특별한 이유가 없으면 따르는 기본값
- `가능하면`, `필요하면`, `권장한다` → 상황에 따라 적용하는 권장사항

### 도구 문서 소유권

도구의 일반적인 소개와 사용법을 작성하거나 수정하기 전에 devkit의 해당
cheatsheet를 확인한다. 설치·설정·명령·옵션·사용 예·문제 해결의 원본은
devkit에서 관리하고, 블로그에는 비교·선택·적용 경험과 글에 필요한 최소 예시만
남긴다.

### 문서 작성 전 규칙 확인

문서 생성·수정을 시작하기 전에 적용되는 규칙 문서를 **실제로 읽고 확인한다.** 규칙 문서가 다른 상위 공통 규칙을 참조하면 링크가 있다는 사실만 확인하고 넘어가지 말고, 참조된 규칙까지 확인한다.

기본 확인 순서는 다음과 같다.

1. [`docs/WRITING_STYLE.md`](docs/WRITING_STYLE.md) — 공통 문체·용어·표기
2. [`docs/DOCUMENT_TYPES.md`](docs/DOCUMENT_TYPES.md) — 문서의 주 유형과 구조
3. [`docs/KNOWLEDGE_ORGANIZATION.md`](docs/KNOWLEDGE_ORGANIZATION.md) — 지식 구성·중복·Knowledge/Blog 역할
4. 대상 경로의 하위 작성 규칙 — 영역별 특수 목적·범위·학습 방식
5. 기존 대상 문서 — 이미 사용 중인 용어, 설명 방식, 이해 흐름

하위 규칙이 공통 규칙을 참조하고 있더라도 **참조만으로 확인을 대신하지 않는다.** 이미 자연스럽게 사용하고 있고 기술적으로 문제가 없는 용어는 임의의 쉬운 표현이나 번역어로 치환하지 않는다.

특히 `_archive/정보관리기술사/` 아래 문서는 일반 블로그 글과 목적이 다르므로 해당 문서 종류의 전용 규칙을 추가로 확인한다. 깊이, 기억·인출 장치, 가지 주제 관리, Diagram처럼 해당 영역에만 필요한 상세 규칙은 각 전용 규칙 문서가 소유한다.

- `개념지도/` → `00-개념지도-공통원칙.md`
- `세부학습/` → `00-세부학습-작성규칙.md`, 필요 시 `00-수식-특수기호-표기규칙.md`
- 그 밖의 기술사 문서 → 해당 경로에 있는 공통·전용 원칙

## Safe Editing

기존 파일 수정에서는 **원문 보존을 최우선**으로 한다.

- 일부 구간만 수정하는 요청이면 해당 구간만 바꾸고, 나머지 본문을 요약·축약·재작성하지 않는다.
- **짧은 파일**은 전체 원문을 완전히 확보한 경우 전체 교체 방식으로 수정할 수 있다.
- **긴 파일**은 필요한 구간만 정확히 확인하고 **부분 수정 방식을 우선**한다. 전체 교체가 꼭 필요하면 구간별 조회, 정상 Blob/Commit 등으로 **파일 끝까지 포함한 완전한 원문을 먼저 확보한 뒤** 수행한다.
- **긴 파일에서는 `update_file` 같은 전체 교체 API를 기본적으로 사용하지 않는다.** 부분 수정 API가 없으면 정상 Blob/Tree/Commit을 이용하는 Git 수준의 안전한 방법을 우선하고, 완전한 원문을 검증할 수 있을 때만 예외적으로 전체 교체한다.
- 파일이 긴지 짧은지 애매하면 긴 파일로 취급하고 보수적으로 수정한다. 읽기 응답의 분량이 작다는 이유만으로 실제 파일도 짧다고 가정하지 않는다.
- Tool이나 API 응답이 `truncated`, 일부 line만 반환, 중간 생략 등으로 **전체 파일인지 확실하지 않으면 그 내용을 전체 원문으로 간주하지 않는다.**
- 전체 파일을 교체하는 API를 사용할 때는 쓰기 전에 **완전한 현재 원문을 확보했는지 확인**한다. 완전한 원문을 확보하지 못했으면 전체 교체를 수행하지 않는다.
- 긴 문서가 잘렸다고 의심되면 먼저 현재본과 Git history를 비교한다. 이전 정상본이 있으면 원래 내용을 복구하고, 최초 생성부터 잘린 경우에만 문맥을 근거로 새 결말을 작성한다.
- 기존 상세 문서를 짧은 요약본으로 바꾸지 않는다. 문서 축약은 사용자가 명시적으로 요청한 경우에만 수행한다.
- **쓰기 직후 Commit diff/stat을 확인한다.** 요청이 일부 구간 수정인데 수십~수백 줄 삭제, 파일 크기 급감, 예상 밖의 대량 변경이 보이면 성공으로 간주하지 말고 즉시 복구한다.
- 수정 전후 diff에서 요청 범위와 무관한 대량 삭제·대량 축약이 발생하면 작업을 중단하고 원인을 확인한다.
- 쓰기 후에는 파일의 **시작부, 수정 구간, 끝부분**을 다시 읽어 원문 손실과 문장 절단이 없는지 확인한다.
- Markdown 문서는 쓰기 후 Code Fence, Table, List, Heading이 중간에 끊기지 않았는지도 확인한다.
- 예상하지 못한 내용 손실이 발생하면 추가 수정을 이어가지 말고 직전 정상 Commit/Blob을 기준으로 즉시 복구한다.
